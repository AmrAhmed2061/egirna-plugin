function updateCount($, $textarea) {
    "use strict";

    var iCount = parseInt($textarea.val().length, 10);
    iRemaining = 160 - iCount;

    $textarea
        .next()
        .children('.egirna_plugin_count')
        .text(iRemaining);
}

function updateTextareas($, $textareas) {
    "use strict";

    $textareas.each(function () {

        updateCount($, $(this));
        $(this).keyup(function () {
            updateCount($, $(this));
        });
    });
}


(function ($) {
    "use strict";
    $(function () {

        updateTextareas($, $('.egirna_plugin textarea'));

        $(document).ajaxSuccess(function () {

            updateTextareas($, $('.egirna_plugin textarea'));

        })

        // Loop through Front To Back textareas

        // For each texterea, detremaine how many characters exist

        // Substract that from 160

        // render it in span element


    });
}(jQuery));